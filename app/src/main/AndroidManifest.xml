<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.comli.shapira.aroundme">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDnTOAuzpUlLxbtctr2Pn59ATGqC4_HxWE" />

        <activity
            android:name="com.comli.shapira.aroundme.activities_fragments.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.default_searchable"
                android:value=".MainActivity" />
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity
            android:name="com.comli.shapira.aroundme.activities_fragments.SettingsActivity"
            android:label="@string/settings_name"
            android:parentActivityName="com.comli.shapira.aroundme.activities_fragments.MainActivity"
            android:theme="@style/SettingsTheme" />

        <activity
            android:name="com.comli.shapira.aroundme.activities_fragments.MapActivity"
            android:label="@string/map_name"
            android:parentActivityName="com.comli.shapira.aroundme.activities_fragments.MainActivity"
            android:theme="@style/MapTheme" />

        <service
            android:name="com.comli.shapira.aroundme.services.NearbyService"
            android:exported="false" />

    </application>

</manifest>